<% if resource.errors[attribute_name].present? %>
  <ul class="ml-3 pb-2 list-disc">
    <% resource.errors.messages[attribute_name].each do |message| %>
      <li class="text-red-500 text-xs"><%= message %></li>
    <% end %>
  </ul>
<% end %>

