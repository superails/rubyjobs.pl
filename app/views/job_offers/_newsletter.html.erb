<%= form_with url: newsletter_subscriptions_path, class:"w-full sm:w-1/2 lg:w-1/3 px-4 mb-12" do |form| %>
  <div class="h-full flex flex-col border bg-red-100 p-6 justify-between">
    <div class="flex flex-col items-center text-center mb-8">
      <p class="text-2xl mb-6 font-bold">Przegląd ofert</p>
      <p class="mb-8">Zapisz się na cotygodniowy przegląd ofert wysyłany na maila.</p>
      <%= form.email_field :email, class: "border px-4 py-2 self-stretch bg-gray-100 focus:bg-white", placeholder: "Adres email" %>
    </div>
    <%= form.submit "Zapisz się", class: "bg-red-500 text-white font-bold text-xl py-3" %>
  </div>
<% end %>
